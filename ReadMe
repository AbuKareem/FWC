if you want to Pull the docker image only, you can Pull it from docker hub via this command: docker pull abukareem/fwc

otherwise you can either use docker-compose.yml or config.sh to do the configuraion.

the preferable way is to use config.sh file and follow the guided setup😉
